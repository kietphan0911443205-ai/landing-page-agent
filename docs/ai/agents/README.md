# Agent contracts

Đây là bản public tối thiểu của Landing Page Agent. Contract chính nằm tại
`landing-page-agent.md`; registry public nằm tại `public-registry.yaml`.

Mọi customer job phải độc lập. Không dùng lại Stitch project, Vercel project,
database, secret scope hoặc PII giữa các khách hàng.
